name: temp-proxy
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
#       - name: Download V2ray
#         run: |
#           wget https://github.com/v2fly/v2ray-core/releases/download/v4.44.0/v2ray-linux-64.zip
#           unzip -o v2ray-linux-64.zip -d v2ray-linux-64/
#           cd v2ray-linux-64
#           chmod +x v2ray
#           echo '${{ secrets.CONFIG }}' > config.json
      - name: Download Cloudflare Tunnel
        run: |
          wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          ./cloudflared-linux-amd64
          ls
#       - xx:
#           wget https://github.com/cloudflare/cloudflared/releases/download/2022.4.1/cloudflared-fips-linux-amd64.deb
#           dpkg -i ./cloudflared-fips-linux-amd64.deb
#       - name: Run V2Ray And Cloudflare Tunnel
#         run: |
#           ./v2ray-linux-64/v2ray & cloudflared tunnel --url 127.0.0.1:16823
